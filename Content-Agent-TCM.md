# 🤖 HeimaEden 独立站 Content-Agent (TCM 组合) 自动化孵化流水线 SOP

> **工具链架构**：`Claude Code (本地 CLI 执政官)` ➔ `MiniMax-M3 (默认 tier)` ➔ `Claude Opus 4.6 (翻译/SEO tier)`
> **核心原则**：最大化利用 Claude Code 的本地文件修改与终端控制权限，由 AI 自动完成创建、局部 debug 和本地测试编译；人类专注搞定“硬核实操测试”、“真机截图”与“最终线上 Git 推送审核”。

---

## 🛠️ TCM 自动化流水线六步走规范

### 📅 阶段一：选题拦截与商业埋点确认（Agent 联网搜索 + 确认）
*   **第 1 步**：在本地终端项目根目录下，启动你的 Claude Code。输入：
    ```bash
    /search 探讨关于 [你感兴趣的技术/软件/机房] 的欧美谷歌高Buyer Intent长尾 How-To 选题
    ```
*   **第 2 步**：Claude Code 默认调用 MiniMax-M3 吐出 3 个高转化长尾标题。
*   **第 3 步**：你直接在终端回复选择其中一个（例如：`"选择第1个，开始生成中文初稿。"`）。

### 📝 阶段二：本地文件全自动创建与中文初稿注入（Agent 自动写盘）
*   **第 4 步**：你无需手动新建文件。直接对 Claude Code 下令：
    ```text
    在 content/posts/ 对应的子目录下，为这个选题全自动创建一个合规的 Markdown 文件。先用中文输出包含硬核步骤的初稿。
    ```
*   **第 5 步**：Claude Code 会在本地**全自动创建 `.md` 文件**。
    *   它会根据根目录下 `CLAUDE.md` 的约束，自动格顶写出完美的 `+++` TOML 头部元数据。
    *   自动在文中埋伏好 `📸 [此处必须强行插入你在电脑上看到的 XXX 后台截图，图片重命名为 xxxx.png]` 和 `🎁 [联盟变现链接占位符]`。

### 🛠️ 阶段三：真机肉身实操与本地 Agent 结对实时 Debug（终极抓虫）
*   **第 6 步**：你打开电脑旁的其他窗口，跟着本地刚刚被 AI 生成的中文 `.md` 步骤开始实操服务器。
*   **第 7 步**：**【核心改变】** 遇到任何终端报错或卡壳，**无需复制粘贴！** 直接在 Claude Code 终端里盲打：
    ```text
    我的终端报错了，提示 [粘贴或者直接让它感知当前目录下的错误日志]，帮我直接在本地修复这个问题，或者告诉我下一步该在服务器敲什么命令。
    ```
*   **第 8 步**：Claude Code 会自己去修改你本地的配置文件，或者直接在终端为你提供通关指令。你顺手把截好的图放进 `assets/images/<category>/<slug>/` 目录下（CLAUDE.md §3.3.1）。

### 🎨 阶段四：血泪踩坑经验自动重组与第二版中文美化（资产无缝揉合）
*   **第 9 步**：当流程跑通后，在终端对 Claude Code 说：
    ```text
    流程已成功跑通。把我刚才遇到的所有报错和纠错经验，连同 assets/images/ 下的截图标签，全部重构揉进这篇 Markdown 的指定章节中，生成第二版高人味中文润色稿。
    ```
*   **第 10 步**：Claude Code 自动在本地重写并更新该 `.md` 文件，将踩坑痛苦原地转化为独创高价值资产。

### 🌐 阶段五：纯正极客英文全量翻译与本地合规清洗（Agent 自动翻译）
*   **第 11 步**：在终端对 Claude Code 下达全量出海翻译指令：
    ```text
    对当前的第二版中文内容进行全量翻译。必须使用最地道、符合欧美极客社区阅读习惯的英文。确保无任何中文字符，确保元数据 TOML 等号格式，发布日期设为过去。
    ```
*   **第 12 步**：Claude Code 在本地自动将整篇 `.md` 洗成纯正、高 SEO 权重的全英文操作教程。

### 🚀 阶段六：本地全自动清空编译与人工最终轰推（人类最终审核）
*   **第 13 步**：在终端对 Claude Code 下达本地测试准备指令：
    ```text
    帮我彻底清空本地 public 和 resources 缓存，并强行拉起 hugo server 服务，我要进行最终视觉核对。
    ```
*   **第 14 步**：Claude Code 会在本地自动执行 `rm -rf public` 并为你启动 `hugo server --disableFastRender`。
*   **第 15 步**：你打开无痕浏览器访问 `http://localhost:1313`，人工核对圆角、极客绿和换肤视觉。
*   **第 16 步**：**【最终安全防线】** 核对完全完美后，你**亲自**在电脑终端输入 Git 连招，全球发布：
    ```bash
    git add .
    git commit -m "feat: deploy elite article via TCM automated pipeline"
    git push origin main
    ```

---

## ⚠️ TCM 组合专属提示词约束声明

当你启动 Claude Code 并在 TCM 语境下与模型对话时，请直接在首次会话中追加以下**”系统级同步约束”**，防止多 tier 混淆：

```text
【TCM 组合最高执行准则】
1. 你当前运行在 Claude Code 本地 CLI 环境中，默认 tier 是 MiniMax-M3，翻译/SEO tier 是 Claude Opus 4.6。请确保所有 Tool-Use（工具调用）输出的 JSON 格式 100% 严丝合缝，绝不夹带任何破坏终端解析的解释性废话。
2. 任何时候修改本地 `.md` 文件，必须确保 Front Matter 使用 +++ 和 TOML 等号语法，绝对禁止输出 YAML 冒号语法。
3. hasCJKLanguage 已经在网站全局关闭，最终英文产出阶段，严禁夹带任何中文字符和中文标点。
4. 所有的图片相对引用路径必须直接指向 '/images/xxxx.png'，严禁夹带 'static/' 前缀。
```
